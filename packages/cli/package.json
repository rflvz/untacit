{
  "name": "@untacit/cli",
  "version": "0.1.0",
  "license": "MIT",
  "description": "untacit CLI: init | import | index | stats | search | diff | conflicts | serve-mcp | extract | update",
  "type": "module",
  "bin": {
    "untacit": "./dist/bin.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@untacit/core": "workspace:*",
    "@untacit/extractors": "workspace:*",
    "@untacit/mcp": "workspace:*",
    "commander": "^12.1.0",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^3.0.0"
  }
}
